---
title: "eigen vector, eigen value 찾는 법에 대한 gpt의 좋은 설명"
date: "2026-02-25"
---

eigen vector, eigen value 찾는 법에 대한 구체적인 설명

1. 고유값(eigenvalue)과 고유벡터(eigenvector)의 출발점
1.1. 기본 정의
우선, ‘고유값’과 ‘고유벡터’의 정의를 다시 한 번 명확하게 적어봅시다.

어떤 
𝑛
×
𝑛
n×n 행렬 
𝐴
A가 있을 때, 0이 아닌 벡터 
𝑣
≠
0
v

=0가
𝐴
𝑣
=
𝜆
𝑣
Av=λv
라는 식을 만족한다면,
𝜆
λ를 
𝐴
A의 고유값(eigenvalue),
𝑣
v를 
𝐴
A의 고유벡터(eigenvector) 라고 부릅니다.
𝑣
≠
0
v

=0인 것은 “영벡터는 방향을 논할 수 없기” 때문입니다.
이는 “행렬(선형변환) 
𝐴
A가 벡터 
𝑣
v를 변환해도, 벡터의 방향은 바뀌지 않고 길이(혹은 부호, 방향을 반대편으로 뒤집는 정도)만 바뀐다”는 것을 의미합니다.

1.2. 식을 
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
(A−λI)v=0로 바꾸어 쓰기
원 정의인

𝐴
𝑣
=
𝜆
𝑣
Av=λv
에서 모든 항을 왼쪽으로 이끌면,

𝐴
𝑣
−
𝜆
𝑣
=
0
⟹
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0.
Av−λv=0⟹(A−λI)v=0.
이렇게 “
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
(A−λI)v=0” 꼴로 바뀝니다.

(1) 
𝑣
≠
0
v

=0 인데도 
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
(A−λI)v=0 가 가능하려면?
여기서 중요 포인트는

𝑣
≠
0
(non-zero) 이면서도
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
v

=0(non-zero) 이면서도(A−λI)v=0
라는 벡터 
𝑣
v가 존재해야 한다는 것입니다.

일반적으로, 어떤 행렬 
𝑀
M에 대해 “
𝑀
𝑥
=
0
Mx=0을 만족하는 
𝑥
≠
0
x

=0가 있다”는 것은, **“행렬 
𝑀
M이 한 방향(또는 여러 방향)을 ‘완전히 0벡터로 매핑’한다”**는 의미입니다.
즉, 
𝑀
M이 100% ‘쪼그라뜨릴(squash)’ 방향이 존재한다는 말과 같습니다.
만약 
𝑀
M이 역행렬이 존재할 정도로 ‘완전히 뒤틀지만 줄어들지 않는(가역)’ 상황이라면, 
𝑥
=
0
x=0 이외엔 0으로 보내지 않습니다. 즉, 가역 행렬은 ‘모든 벡터 
≠
0

=0’를 0으로 만들 수 없습니다.
따라서, “
𝑣
≠
0
v

=0이면서 
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
(A−λI)v=0”가 가능하려면, 
(
𝐴
−
𝜆
𝐼
)
(A−λI)는 가역이 아니어야 합니다. (Non-invertible, 즉 ‘singular’ 해야 한다.)

2. 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0의 등장
2.1. 가역성과 행렬식(det)의 관계
선형대수에서 **“행렬이 가역이다”**는 말은 곧 **“그 행렬의 행렬식(det)이 0이 아니다(
≠
0

=0)”**라는 사실과 동치입니다.

어떤 정방행렬 
𝑀
M에 대해,
det
⁡
(
𝑀
)
≠
0
det(M)

=0 이면, 
𝑀
M은 가역(invertible)
det
⁡
(
𝑀
)
=
0
det(M)=0 이면, 
𝑀
M은 비가역(singular, 즉 역행렬이 존재하지 않음)
이것은 선형대수의 핵심 정리 중 하나입니다.

그렇다면, 
(
𝐴
−
𝜆
𝐼
)
(A−λI)가 비가역이어야(=행렬식=0이어야)만 
𝑣
≠
0
v

=0 해를 가질 가능성이 생깁니다.

2.2. 결론: 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0 → 고유값 
𝜆
λ
정리하자면,

(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
(A−λI)v=0 에서 
𝑣
≠
0
v

=0 이라는 조건이 필요.
(
𝐴
−
𝜆
𝐼
)
(A−λI)가 가역이면 
𝑣
=
0
v=0만 가능 → 우리가 원하는 “
≠
0

=0 해”는 절대 없음.
따라서 
(
𝐴
−
𝜆
𝐼
)
(A−λI) 비가역 → 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0.
즉, **“
𝑣
≠
0
v

=0인 고유벡터”**가 존재하기 위한 필수·충분조건이 “
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0”입니다.
그래서 결국 **고유값 
𝜆
λ**를 찾으려면,

det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0
이 되는 
𝜆
λ를 풀어서 구하게 됩니다.

3. 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0를 푸는 과정: 특성다항식(Characteristic Polynomial)
3.1. 특성다항식의 정의
det
⁡
(
𝐴
−
𝜆
𝐼
)
det(A−λI)는 
𝜆
λ에 관한 다항식(polynomial)입니다.
이 다항식을 보통 “행렬 
𝐴
A의 특성다항식(characteristic polynomial)”이라고 부르며,
𝑝
𝐴
(
𝜆
)
=
det
⁡
(
𝐴
−
𝜆
𝐼
)
.
p 
A
​
 (λ)=det(A−λI).
라고 표기하곤 합니다.
예: 2×2인 경우
2차원 행렬

𝐴
=
(
𝑎
𝑏
𝑐
𝑑
)
A=( 
a
c
​
  
b
d
​
 )
에 대해서는

det
⁡
(
𝐴
−
𝜆
𝐼
)
=
det
⁡
(
𝑎
−
𝜆
𝑏
𝑐
𝑑
−
𝜆
)
=
(
𝑎
−
𝜆
)
(
𝑑
−
𝜆
)
−
𝑏
𝑐
.
det(A−λI)=det( 
a−λ
c
​
  
b
d−λ
​
 )=(a−λ)(d−λ)−bc.
이것을 전개하면

𝜆
2
−
(
𝑎
+
𝑑
)
𝜆
+
(
𝑎
𝑑
−
𝑏
𝑐
)
λ 
2
 −(a+d)λ+(ad−bc)
꼴이 되고, 이걸 0으로 두면 2차 방정식이 되어, 
𝜆
λ를 해로 구할 수 있습니다. 그 해(근)가 곧 고유값이 됩니다.

예: 3×3 이상의 경우
3
×
3
3×3면 3차 다항식, 
𝑛
×
𝑛
n×n이면 일반적으로 n차 다항식이 나옵니다.
이를 전개하고 해를 구하는 것은 복잡할 수 있으나, 원리는 동일합니다.
다항식의 근(해)이 곧 고유값이 됩니다.
3.2. 왜 “
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0인 
𝜆
λ”가 중요한가?
이것을 다시 한 번 다른 관점에서 보면,
𝑝
𝐴
(
𝜆
)
=
det
⁡
(
𝐴
−
𝜆
𝐼
)
p 
A
​
 (λ)=det(A−λI)
는 행렬 
𝐴
A가 
𝜆
λ만큼의 스케일을 빼 주었을 때(= 
𝐴
−
𝜆
𝐼
A−λI), 그 변환이 한 방향을 완전히 0으로 누를(squash) 수 있는가를 보여주는 지표입니다.
𝑝
𝐴
(
𝜆
)
=
0
p 
A
​
 (λ)=0이면, 그 스케일 
𝜆
λ를 가지는 벡터들이 “그 변경(
𝐴
−
𝜆
𝐼
A−λI)에서 완전히 0이 되는(매핑 결과가 0)” 특별한 방향이 생깁니다.
즉, 그 방향은 원래의 
𝐴
A에서 보면, “
𝜆
λ 배만큼 늘어나거나 줄어드는” 상태이므로, 고유값/고유벡터가 됩니다.
4. 고유값을 이렇게 찾는 직관적 이해
4.1. 선형변환의 “방향 유지” 개념
행렬 
𝐴
A를 선형변환이라고 해석해봅시다. 2차원·3차원에서 마치 “어떤 물체나 공간을 회전/늘이기/뒤틀기/뒤집기” 하는 역할입니다.

일반적으로는, 임의의 벡터 
𝑣
v가 
𝐴
𝑣
Av로 가면, 방향도 바뀔 수 있고, 길이도 바뀔 수 있습니다.
하지만 어떤 ‘특별한 방향’은, 변환 후에도 그 방향이 그대로 유지될 수 있습니다. (혹은 180도 반대만 날 수도 있음. 이때도 같은 선상(직선) 위에 있음을 의미.)
이때 길이는 
𝜆
λ배로 변하고, 방향은 동일. 그것이 바로 고유벡터이고, 
𝜆
λ가 고유값입니다.
4.2. (A - λI)로 보면 무슨 일이 일어나나?
𝐴
A가 벡터를 어떻게 바꾸는지에서, “
𝜆
λ배 변하는 것”을 중립화(Shift) 해보는 것입니다.
예컨대, 
𝑣
v가 
𝜆
λ배 커지는 효과를 없애주려면, 변환에 
−
𝜆
𝐼
−λI를 추가(빼기)하여 그 부분을 상쇄해버릴 수 있습니다.
만약 진짜로 
𝑣
v가 그 방향이었다면, 새롭게 정의된 연산 
(
𝐴
−
𝜆
𝐼
)
(A−λI)는 
𝑣
v를 “0으로” 보내는 일이 벌어집니다.
그런데, 어떤 선형변환이 ‘
≠
0

=0인 벡터를 0으로 보낼 수 있다’면, 그것은 “해당 선형변환이 1대1이 아님(가역이 아님)”을 의미합니다.
다시 말해, 그 변환은 자기 자신만의 결함(?) 때문에 어느 특정 방향을 완전히 붕괴시키는 것이죠.
행렬적으로 표현하면, 그 변환의 **행렬식(det)**이 0입니다.
즉, “
𝜆
λ를 제대로 골랐을 때, (A - \lambda I))가 특정 방향을 0으로 보낼 수 있게 되고, 그 방향이 우리가 찾던 고유벡터가 된다!”라는 것입니다.

(A - λI)가 비가역
⟺
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
⟺
𝑣
≠
0
 인 
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
 존재
.
(A - λI)가 비가역⟺det(A−λI)=0⟺v

=0 인 (A−λI)v=0 존재.
5. “고유값 = 행렬의 특성”이라는 더 큰 의미
5.1. 대각화(Diagonalization)와 고유공간
고유값들이 가지는 중요한 특징 중 하나는, 행렬을 “더 간단한 형태로 바꾸는(대각화)” 데 있어 핵심적인 역할을 한다는 점입니다.

예를 들어, 행렬 
𝐴
A가 서로 다른 
𝑛
n개의 고유값과 고유벡터들을 가지는 경우, 적절히 “고유벡터로 이루어진 행렬”을 만들면 
𝐴
A를 대각행렬로 바꿀 수 있습니다. (즉, 
𝑃
−
1
𝐴
𝑃
=
𝐷
P 
−1
 AP=D, 여기서 
𝐷
D는 대각행렬)
그 대각행렬의 대각성분이 바로 “고유값”들이 됩니다.
결과적으로 고유값은 “행렬이 본질적으로 하는 변환에서 가장 핵심 축(혹은 방향)과, 그때의 배율”을 나타내며, 행렬의 구조를 이해하는 가장 기본이 됩니다.

5.2. 물리학·공학·데이터 분야에서의 활용
물리: 양자역학에서 해밀토니안(Hamiltonian)의 고유값은 물리계의 에너지를 의미, 고유벡터(고유상태)는 해당 에너지의 상태 등.
공학: 진동/회전 시스템에서, 고유값은 “고유진동수”가 되고, 고유벡터는 “진동모드” 방향이 됩니다.
데이터분석: PCA(주성분분석)에서 공분산 행렬의 고유벡터는 가장 분산이 큰 방향(주성분 축), 고유값은 그 분산의 크기를 가리킵니다.
이처럼 고유값을 구하는 것은 행렬이 (또는 어떤 시스템이) 어떠한 성질을 갖는지를 파악하는 아주 기본적이면서도 강력한 도구입니다.

6. 한 번 더 정리: “왜 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0로 
𝜆
λ를 찾는가?”
고유값의 조건: 
𝐴
𝑣
=
𝜆
𝑣
Av=λv (단, 
𝑣
≠
0
v

=0).
이를 
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
(A−λI)v=0로 바꾼다.
𝑣
≠
0
v

=0인 해가 존재하려면, 
(
𝐴
−
𝜆
𝐼
)
(A−λI)가 
𝑣
≠
0
v

=0을 0으로 보낼 수 있어야 한다.
이는 
(
𝐴
−
𝜆
𝐼
)
(A−λI)가 선형변환으로서 “모든 방향을 독립적으로 보존”하지 못하고 특정 방향을 ‘붕괴(=0)’시키는 상황임을 뜻한다.
선형대수적으로, 행렬이 특정 
≠
0

=0 벡터를 0으로 보내려면 역행렬이 없어야(가역성 결여) 한다.
행렬의 역행렬 존재 여부는 행렬식이 0인지 아닌지로 결정된다.
결론: 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0.
따라서 “
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0이 되는 
𝜆
λ”가 바로 “
𝐴
A의 고유값”이 된다.
이 과정이 논리의 전부입니다. 즉, 고유벡터 
𝑣
≠
0
v

=0가 생기려면, 
(
𝐴
−
𝜆
𝐼
)
(A−λI)가 비가역이어야 하고, 그 비가역성은 **
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0**과 동일하므로, 그 식을 통해 
𝜆
λ를 구한다는 것입니다.

7. 결론 및 요약
정의에서 시작:

𝐴
𝑣
=
𝜆
𝑣
,
𝑣
≠
0
⟺
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
,
𝑣
≠
0.
Av=λv,v

=0⟺(A−λI)v=0,v

=0.
𝑣
≠
0
v

=0를 0으로 보내는 변환은 
(
𝐴
−
𝜆
𝐼
)
(A−λI)가 가역이 아님을 뜻한다.

가역이면 
𝑣
=
0
v=0밖에 해가 없기 때문.
가역 아님 ↔ 행렬식 0

행렬식이 0이면 역행렬이 존재하지 않는다는 것이 선형대수의 기초정리.
결론:

(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
(
𝑣
≠
0
)
⟺
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0.
(A−λI)v=0(v

=0)⟺det(A−λI)=0.
고로 고유값 
𝜆
λ는 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0을 만족시키는 값으로 정의·계산된다.
특성다항식:

det
⁡
(
𝐴
−
𝜆
𝐼
)
det(A−λI)을 전개해 만든 다항식을 ‘특성다항식’이라고 하며, 이를 0으로 두어 **근(roots)**을 구하면 그것이 고유값이 된다.
의미:

고유값·고유벡터는 **“행렬(선형변환)이 어떤 축/방향을 어떻게(몇 배로) 늘이거나 줄이는가”**를 나타내며, 행렬을 ‘안에서부터 이해’하는 데 가장 중요한 개념이 된다.
(부록) 추가적인 비유 혹은 예시
러그(카펫)를 잡아당기는 상상:
2D 평면의 한 점(벡터) 
𝑣
v가 있다고 합시다. 러그를 잡아당기는(=행렬 
𝐴
A로 표현되는) 동작을 하면, 대부분의 점은 복잡하게 움직일 것입니다(회전+확대/축소).
그러나 어떤 점(혹은 방향)은 줄이거나 늘려도 방향이 그대로 유지되는 구간이 있을 수 있습니다(가령 특정 선 위에서만 밀면 “평행 이동”처럼 보이는 경우 등). 그 점/방향이 바로 “방향 유지 = 고유벡터”에 해당합니다.
그리고 이때 길이가 얼만큼 바뀌는지가 “고유값”입니다.

문자열(스프링)의 공진 주파수:
스프링, 혹은 기타 줄을 흔들면, 특정 진동수에서 강하게 진동합니다. 이 “특정 진동수(=고유진동수)”가 바로 고유값에 해당하고, 그때 줄의 특정 패턴(배 마디 형태)이 고유벡터에 해당한다고 볼 수 있습니다.
이 “특정 주파수”를 찾는 과정에서 결국 어떤 “미분방정식 → 행렬”을 세웠을 때, 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0 꼴이 나오고, 그 값이 물리적 고유진동수가 됩니다.

이처럼 다양한 맥락에서, “어떤 시스템에 ‘특별한 값(
𝜆
λ)’이 존재하고, 그것이 시스템을 가장 단순하고도 핵심적으로 표현”한다는 점이 고유값 이론의 큰 의의입니다.

마무리
왜 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0을 통해 
𝜆
λ를 찾는가?
고유벡터 
𝑣
≠
0
v

=0가 존재하려면 
(
𝐴
−
𝜆
𝐼
)
𝑣
=
0
(A−λI)v=0의 해가 non-zero여야 하고, 이는 
(
𝐴
−
𝜆
𝐼
)
(A−λI)가 비가역(행렬식 0)인 조건과 동치이기 때문입니다.
결국 
𝜆
λ는 “
(
𝐴
−
𝜆
𝐼
)
(A−λI)를 비가역으로 만드는 값”, 즉 선형변환 
𝐴
A가 “
𝜆
λ 배로 늘이거나 줄이는” 특별한 비율을 뜻합니다.
이렇게 구한 고유값과 그에 대응하는 고유벡터는, 행렬의 내재적 특성을 보여주고, 대각화·PCA·공진현상 등 선형대수 전반에서 아주 핵심적인 역할을 합니다.
이로써 “
𝜆
λ를 논리적으로 어떻게 추론해내며, 왜 
det
⁡
(
𝐴
−
𝜆
𝐼
)
=
0
det(A−λI)=0이 고유값 조건이 되는가”에 대한 최대한 상세한 답변이 되었기를 바랍니다.
추후 예시(특정 숫자 행렬에 대해 실제로 
det
⁡
(
𝐴
−
𝜆
𝐼
)
det(A−λI) 구해보고, 고유값·고유벡터를 확인)를 따라 해보시면, 더욱 실감 있게 이해하실 수 있습니다.